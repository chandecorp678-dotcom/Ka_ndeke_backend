// routes.js — compatibility shim that delegates to routes-index.js
module.exports = require('./routes-index');

// routes.js — compatibility shim that delegates to latest-gameEngine.js
module.exports = require('./latest-gameEngine.js');